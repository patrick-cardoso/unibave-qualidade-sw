package br.unibave.bpr.exercicios2_3_3;import java.io.File;import java.io.FileWriter;import java.io.IOException;import java.util.Date;public class LogFile extends AbstractLogTool {	private File arquivoLog;	public LogFile() {		// Localiza o arquivo de log.		String pkg = getClass().getPackage().getName().toString().replace('.', '/');		File dir = ResourceUtils.getResourceAsFile(pkg + "/");		arquivoLog = new File(dir, "log.txt");		try {			if (!arquivoLog.exists())				arquivoLog.createNewFile();		}		catch (IOException e) {			System.out.println("Não foi possível criar o arquivo de log:\n" + e);			System.exit(1);		}	}	public void logRegister(String msg) {		try {			// Imprime no arquivo.			FileWriter writer = new FileWriter(arquivoLog, true);			writer.write(dateFormat.format(new Date(System.currentTimeMillis())) + msg + "\n");			writer.close();		}		catch (IOException e) {			System.out.println("Não foi possível escrever no arquivo de log:\n" + e);			System.exit(1);		}	}}