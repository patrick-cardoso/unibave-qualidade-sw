package br.unibave.bpr.exercicios2_3_2;import java.util.Arrays;public class Main {	public static void main(String[] args) throws Exception {		boolean secure = false;		if (args.length >= 1) {			secure = Arrays.stream(args).anyMatch(s->s.equals("designpatterns"));		}		AbstractProvider provedor = secure ? new Confidential() : new Public();		provedor.showInfo();	}}